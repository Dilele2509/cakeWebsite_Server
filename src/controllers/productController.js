'use strict';

const userData = require('../data/roles'); //đổi tên biến tương tự với tên file tương ứng

/* viết hàm tương tự với roleController */

module.exports = {
    /* viết xong r export nó ra đây */
}